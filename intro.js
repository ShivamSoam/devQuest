let obj = {
    "name" : ["Shivam", "Shrohit"],
    "sname" : ["Soam" , "Soam"]
};

function fun(object)
{
    console.log(object);
    for(let key in object)
    {
        if(object[key].contains())
    }
    //let ans = checkIfExists()
}

let ans = fun(obj);